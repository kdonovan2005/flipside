<%= form_for :user, method: :delete, url: user_path(current_user) do |f| %>
  <h3><%= current_user.name %></h3><br>
  <%= f.submit("Confirm Delete?", class: 'btn btn-danger btn-circle btn-lg') %> <br>
<% end %>
